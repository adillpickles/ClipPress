{
  "name": "clippress-site",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone marketing/download site for ClipPress. No backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "typecheck": "tsc --noEmit",
    "lint": "node scripts/lint.mjs"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.3.5"
  }
}
