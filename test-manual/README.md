Fixtures are stored separately:
https://github.com/mifi/lossless-cut-fixtures

Suggested ClipPress checks:
- Lossless export still behaves like the existing fast clipping workflow when `Lossless export` is selected.
- `Limit file size` creates shareable MP4 output and stays under the requested target in normal cases.
- `Fast` and `High Quality` both finish successfully for single-segment, separate multi-segment, merge, and merge+separate exports.
