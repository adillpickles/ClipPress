Fixtures are stored separately:
https://github.com/mifi/lossless-cut-fixtures

Suggested ClipPress MVP checks:
- Lossless export still behaves exactly like LosslessCut when `Lossless export` is selected.
- `Limit file size` creates MP4 output and keeps files under the requested target in normal cases.
- `Fast` and `High Quality` both finish successfully for single-segment, separate multi-segment, merge, and merge+separate exports.
